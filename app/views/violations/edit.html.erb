<h1>Update Submitted Violation</h1>

<%= form_for(@violation, :html => {multipart: true, class: "form-horizontal", role: "form"}) do |f| %>
  <% if @violation.errors.any? %>
    <%= pluralize(@violation.errors.count, "error") %>
        prohibited this violation from being saved:
            <% @violation.errors.full_messages.each do |msg| %>
              <%= msg %>
              <% end %>
    <% end %>
  <% end %>

<%= form_for(@violation, html: {multipart: true}) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <%= f.label :image, "Attach an Image" %>
  <%= f.file_field :image %>

  <%= f.submit 'Update Violation' %>
<% end %>