<div class='container'>
  <div class='jumbotron'>
    <h1 class="text-center">Account Dashboard</h1>
    <h3>Hello, <%= current_user.name %></h3>
  </div>
</div>

<%= render 'sidebar' %>
<%= render 'table' %>
<%= render 'cards' %>